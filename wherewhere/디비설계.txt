user table : 고유Idx, sns 정보, 토큰, 리프래시 토큰, 회원가입 일자

user product table : 고유Idx, url, 이미지, 리사이징 이미지, 영상, 리사이징 영상?, 가져온 일자, title, 본문내용, 복수여부(이미지, 동영상 등이 1개 이상이다, 아니다 구분), 대분류Idx, 중분류Idx, 상품 중요도(0,1), userId(고유Idx), like(삭제했을때 지우지 않고), recommend, 스폰서
(추천하는 게 : recommend, 스폰서/ 직접 등록한 게 userId -> 고민해보기/ 직접 등록한건지, 추천된건지 구분해야 함.)

all product table : 고유Idx, url, 이미지, 리사이징 이미지, 영상, 리사이징 영상?, 가져온 일자, title, 본문내용, 복수여부(이미지, 동영상 등이 1개 이상이다, 아니다 구분), 대분류Idx, 중분류Idx, 상품 중요도(0,1), like(삭제했을때 지우지 않고)

user action table : 고유Idx, user 고유Idx, user product 고유Idx, 상태값(유저, 추천, 광고), 클릭시간

q&a : 고유Idx, 질문, 답변

.
.
.
paging 처리(더 불러오기.)

aws 환경설정
몽고디비 공부
구글로그인 -> firebase?

test서버, 배포서버, 파일서버 따로 두기

<질문 정리>

구글은 idToken 받은 것부터 처리
페이스북은 클라에서 액세스토큰 받은 것 부터 처리
애플은 

1. 사용자 정보 유지는 그냥 userIdx로 하는건가?
-> 넹

2. 동영상 저장은 어떻게 한다는거임
-> HLS ?

3. 사진 개수가 여러개라는 것은 클라에서 주나?
-> 크롤링한 값을 보내주실 때 피엠님이 같이 보내주실 것이다.

4. 이미지 저장
-> multer로 s3에 저장하고, 이미지 url값을 리턴받아서 바로 디비에 저장